# HANDOFF

最終更新: 2026-08-26

## いま何をしているのか

コムドット切り抜きの在庫作りと予約管理。**このセッションは制作をしていない。アカウントの実態調査をした。**
結果、台帳・引き継ぎに書かれていた前提が実態と食い違っていることが分かった。**制作を再開する前に
下の「次にやること」を片付けること。**

## 今回やったこと（2026-08-26）

コードもデータも変更していない。**読み取りのみ**の調査。HANDOFF.md だけ書き換えた。

1. YouTube API での状態確認を試みた → **リフレッシュトークンが失効していて全 API が使えない**
2. API が死んでいるので、**ログアウト状態のブラウザ**でチャンネルの公開ページを実測した
3. 実測結果を `data/videos/*.json` と `config/schedule.yaml` に突き合わせた
4. BRDOCK からの返信有無を Gmail (oshima6.27@gmail.com) で検索した
5. 検索の過程で、**過去に別チャンネルが YouTube から完全削除されている**ことが分かった

## 検証済みの事実（実際に画面に出た出力のみ）

### A. API が使えない

```
google.auth.exceptions.RefreshError:
('invalid_grant: Token has been expired or revoked.', {'error': 'invalid_grant'})
```

- `token.json` の更新日時は 2026-08-14 09:41 JST。以降まったく更新されていない
- `docs/youtube-api-setup.md` 記載の「同意画面がテスト中 → リフレッシュトークンが7日で失効」と時系列が一致
  （08-14 発行 → 08-20 のアップロードは成功 → 08-26 の今日は失効）
- `channels.list`（1ユニット）すら通らない。**クォータではなく認証の問題**

### B. 許諾 pending のまま14本が一般公開されている

ログアウト状態のブラウザで `https://www.youtube.com/playlist?list=UUoT2TYsxzH4t42C2oF-KrAw` を開いて確認。
`loggedIn: false` / `cookieHasSID: false` を確認済み。つまり**誰からでも見える状態**。

- `config/permission.yaml` は `status: pending` / `responded_at: null` のまま
- Gmail を検索したが、**BRDOCK からの返信も、こちらからの依頼の送信記録も見つからない**
  （見つかったのは別件：ひろゆき／令和の虎Second の切り抜き申請＝ガジェット通信 getmcn@razil.jp 経由。承諾済み）

### C. 除外指定の2本が公開されている ← 一番まずい

| YouTube ID | 公開日時(JST) | 再生 | 状況 |
|---|---|---|---|
| cTzbNUFi9Iw | 2026-08-22 12:00 | 5 | 公開中 |
| hbRhnrvPk-k | 2026-08-22 18:00 | 0〜4 | 公開中 |

どちらも素材 `RGm5F2m12as`。**`config/exclusions.yaml` で動画ごと除外済み**で、
`config/schedule.yaml` の orphans に「外部の催眠術師が映っているため対象外」「削除が要る」と書いてある。
08-18 に予約を外したはずが、**08-22 に公開されている＝その後もう一度予約が入れられた。**

### D. 台帳と実態がずれている（台帳20本 = 公開14 + 非公開6）

- `data/videos/*.json` は**20本すべて `private`** と記録。実際は14本が公開
- `config/schedule.yaml` の `not_scheduled` に残っている2本は、**すでに公開済み**
  - `XT01mLljg6Y`（8DDHTuwdbyQ/auto01）: 2026-08-18 18:30 JST 公開、0回視聴
  - `XPx2y_vDWV8`（51tyFgelkmQ/wide01）: 2026-08-18 18:00 JST 公開、0回視聴、11:25
    （schedule.yaml は「予約が入っていない」「提案 08-26 20:00」と書いているが誤り）
- `slots` の10本は予約どおり全部公開済み
- 非公開のまま残っている6本: `0gWpS9XyIEs` `jr9NXY7P8dk` `TjovR5KnSu4` `MDPhcHIYOL8`
  `CCb0ZcuhoZA` `tUyrCoVoPCM`（`0gWpS9XyIEs` を開いて「非公開動画」表示を確認）

### E. 過去にチャンネルが完全削除されている（アカウント全体のリスク）

Gmail (oshima6.27@gmail.com) に届いた YouTube からの通知。**本文を実際に読んで確認した。**

- 2025-09-18 07:27 「チャンネルが YouTube から削除されました」
  - 対象: `UCfxU85b3C1Zt3w8Nt6TO9zw`「データで暴く政治の闇【日本人ファースト】」
  - 理由: スパム、欺瞞行為、詐欺に関するポリシーの重大な違反または度重なる違反
  - 文面:「お客様は今後、他の YouTube チャンネルにアクセスしたり、チャンネルを所有、作成したりすることはできません」
- 2025-09-18 10:11 再審査請求 → **却下**。「このチャンネルは YouTube に再度公開されません」
- 上記メールの宛名（＝そのアカウントの表示名）は**「コムドットのおもしろ切り抜きチャンネル」**
- 2024-05-09 同アカウント宛「チャンネル履歴が不十分なため上級者向け機能にアクセスできなくなりました」

### F. 数字

- 公開14本の再生数は 0〜6回。登録者数はチャンネルページに表示されない（非表示設定か0）
- 実質、配信されていない状態

## 未検証のもの

- **現行チャンネル `UCoT2TYsxzH4t42C2oF-KrAw` がどの Google アカウントの所有かは未確認。**
  `docs/youtube-api-setup.md` は投稿用アカウントを `orfevre6.27@gmail.com` と書いており、
  2026-08-19 に「orfevre6.27@gmail.com への新しいログイン」「2段階認証プロセスが有効になりました」の
  セキュリティ通知が oshima6.27@gmail.com（再設定用アドレス）に届いている。
  ここから「別アカウントで運用している」と読めるが、**Studio で直接確認していないので断定していない**
- **E が「電話番号確認ができない」理由かどうかは推測にすぎない。** 削除されたアカウントに番号が
  紐づいている可能性はあるが、確かめていない
- **E の削除措置が現行チャンネルに及ぶかどうかは分からない。** YouTube が両アカウントを
  同一人物として紐づけているという証拠は持っていない
- 公開14本のうち10本（slots 分）の実際の公開日時は個別に確認していない。schedule.yaml の予約時刻を
  そのまま信じている。個別に確認したのは C と D の4本だけ

## 次にやること

**制作より先にこれを片付ける。優先順に並べてある。**

### 1. 除外2本を公開停止する（運営者が Studio で手動。API は死んでいる）

`cTzbNUFi9Iw` と `hbRhnrvPk-k`。外部の催眠術師（第三者）が映っており、BRDOCK への
許諾依頼の射程外。非公開に戻すだけでは次の一括操作でまた巻き込まれるので、**削除する**。

### 2. API を復旧する（人がブラウザで行う。無人では通らない）

```bash
cd C:/Users/oshim/Documents/projects/com.-youtube && rm token.json && python -m clipper auth
```

同意画面では必ず「コムドット名場面ch【切り抜き】」を選ぶ。`UCoT2TYsxzH4t42C2oF-KrAw` が
表示されれば正しい。**7日でまた失効する。** 恒久対応は同意画面の本番公開だが Google の審査が要る。

### 3. 台帳を実態に合わせる（API 復旧後）

`data/videos/*.json` の20本と `config/schedule.yaml` を、B〜D の実測値で書き換える。
現状の「全部 private」「XPx2y_vDWV8 は未予約」は誤り。**このずれを残したまま一括操作をしない。**

### 4. 許諾の扱いを決める

依頼文は「ご許諾をいただけた場合にのみ公開いたします」だが、実際には pending のまま14本が公開されている。
Gmail に BRDOCK とのやり取りの記録がない。**依頼を本当に送ったのか、別の手段（フォーム等）だったのかを
確認する。** 未送信なら、公開済み14本の扱いを含めて判断が要る。

### 5. E の影響を確かめる

現行チャンネルの所有アカウントを Studio で確認し、2025-09-18 の削除措置との関係を把握する。
**新規制作を積み増す前にここを見ておく。** 積んでから止まると全部無駄になる。

## 触ってはいけないところ

- **gate を迂回して publishAt を入れない。** Studio での手動予約は運営者の判断領域
- `RGm5F2m12as` / `fbKne9hTmgA` は動画ごと除外（`config/exclusions.yaml`）。予約や一括操作に
  巻き込まないこと。**RGm5F2m12as 由来の2本は現在公開されてしまっている（上記1）**
- `Fb9bO8V9oNA`（目隠しかくれんぼ）は CDF シャッフルコラボ回。素材にしない
- カスタムサムネイル不可・15分超不可（チャンネルの電話番号確認ができないため）。再依頼しない
- `wKuTNfA6Xhg` auto03 は区間取得・第三者点検まで済み、**未書き出し・未アップロード**。
  制作を再開するならここが1本目候補だが、上記1〜5を先に片付けること
