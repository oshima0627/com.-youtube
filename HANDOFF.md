# HANDOFF

最終更新: 2026-09-10

## いま何をしているのか

コムドット切り抜きの制作と投稿。**在庫18本は非公開のまま、8日間1本も出していない。**

いま立っている場所:

- **2026-09-02 以降、公開は0本。** `config/schedule.yaml` の18枠のうち
  **15枠（09-03 07:00 〜 09-10 07:00）が空振りで過ぎた。`--arm` を打っていない**
- 09-04 に立てた「投稿を再開したら配信が増えるか」の検証は、**再開していないので成立しなかった**
- 配信の診断は 09-02 から変わっていない。**ショートフィード経由は28日で約3回**（下の 1）
- **許諾の回答文はいまだに見つかっていない。** `permission.yaml` は `granted` のままで
  裏づけが無い。**投稿本数を増やすほど拡大する risk であり、いま最優先の未解決事項**
- 認証は生きている。**トークンの7日失効は起きなかった**（下の 3。この論点は閉じた）

## 今回やったこと（2026-09-10）

1. **`scripts/account_audit.py` を実行し、API でチャンネルを実測した**（下の 2）
2. **Studio 詳細モードをブラウザで開き、インプレッションを測り直した**（下の 1）
3. **競合3チャンネルの直近8本を `yt-dlp` で取り直した**（下の 4）
4. 記録を [`docs/analytics-2026-09-10.md`](docs/analytics-2026-09-10.md) に新規で書いた

変更したファイル: `docs/analytics-2026-09-10.md`（新規）/ `HANDOFF.md`。
**コードは触っていない。**

## 検証済みの事実（実際に画面に出た出力だけ）

### 1. インプレッション再測（Studio 詳細モード・期間 2026/08/12〜09/08）

| 指標 | 今回 | 前回 2026-09-02（期間 08/04〜08/31） |
|---|---:|---:|
| 視聴回数 | **65** | 45 |
| インプレッション | **467** | 355 |
| サムネイルCTR | **2.4%** | 1.4% |
| 登録者 | **0** | 0 |
| 対象本数 | 16本 | 14本 |

**期間が8日ずれているので差を効果として読まないこと。**
1本あたり 25回/28日 → **29回/28日**。桁は動いていない。

流入経路: YouTube検索 **84.4%** / ブラウジング 6.3% / 直接・不明 4.7% /
**ショートフィード 4.7%（＝ 64回の4.7%でおよそ3回。前回4.6%からまったく変わらず）**。
エンゲージメントは **視聴継続 63.0% / スワイプ 37.0%**（前回 60/40）。

**インプレッション467はフィードの露出ではない。** ショートのサムネイルインプレッションは
検索・ブラウジング面だけを数える。**フィードが出ていない証拠は467ではなく「4.7%＝3回」のほう。**

動画ごとの表は [`docs/analytics-2026-09-10.md`](docs/analytics-2026-09-10.md) の1-1節。

### 2. 09-02 の2本は、公開2日目以降ほとんど伸びていない

`python scripts/account_audit.py` の出力を 09-04 の記録と突き合わせた。

| クリップ | 09-04 | 09-10 | 6日間の増分 |
|---|---:|---:|---:|
| `8DDHTuwdbyQ/auto03` | 8 | 8 | **0** |
| `abW8zkEwEW4/auto06` | 7 | 7 | **0** |
| 公開分の合計 | 60 | 64 | +4 |

**09-04 が疑ったとおり、「再開後の2本は再生/日が17倍」は front-load を見ていただけだった。**
増えた+4は8月公開分に付いている。台帳との突き合わせは34本すべて一致。

**ただし頻度の効果を否定したわけではない。再開そのものをしていない。**

### 3. トークンは7日で失効しなかった

`token.json` は 2026-09-03 発行。**2026-09-10 の今日、再認証なしで API が通った。**
本番環境へ公開した OAuth 同意画面のトークンは7日で切れない。**この未検証事項は解けた。**

### 4. 競合との差は変わっていない（`yt-dlp`、2026-09-10）

直近8本の再生数。**24本の最小が 1,200回**、こちらの最大は10回。

| チャンネル | 直近8本 |
|---|---|
| コムの巣窟 | 3,800 / 2,800 / 334,000 / 217,000 / 13,000 / 242,000 / 38,000 / 70,000 |
| コムドットの元食わず嫌い | 6,300 / 7,200 / 6,900 / 23,000 / 78,000 / 22,000 / 51,000 / 87,000 |
| コムピース | 128,000 / 20,000 / 9,300 / 1,200,000 / 50,000 / 23,000 / 32,000 / 19,000 |

### 5. Studio を開くときの注意（今回判明）

Chrome の既定ログインは **`orfevre6.27@gmail.com`**（別チャンネル）。
そのまま Studio を開くと「このページを表示する権限がありません」になる。
**URL に `?authuser=1` を付ける。** 詳細モードは画面右上のボタンから開く
（`/explore` へ直接 URL で入ると描画されなかった）。

## 未検証のもの

- **投稿頻度を上げると配信が増えるかは、いまだに一度も測っていない**（今回も測れなかった）
- **許諾の回答文を見ていない。** `granted` の根拠は運営者の申告だけ
- **新型式タイトルの2本が CTR 5.9% / 4.8% と上位**だが、母数が34回・42回。**判定できない**
- **尺を短くすると配信が変わるかは未検証。** 競合72本では尺と再生数は無相関（r=0.050）
- **新環境で実際のアップロード・公開を1本も通していない**（`clipper auth` までしか通していない）
- **クォータの実際の上限を確認していない。** 既定なら 10,000ユニット/日
- **`M7ZxIL_b39E` の8本は各6コマしか見ていない**（第三者は48コマで0人。通しでは見ていない）
- 書き出していない `auto09`〜`auto12` は1コマも見ていない
- **アップロード済み6本のタイトルは旧型式**（メンバー名・`#shorts` なし）
- `fDiW0YbLd-Q` の飲酒が年齢制限を受けるかは未確認
- **`G2gXaVSnOQY`（北海道前編）は未着手。** すすきの回なので第三者リスクが高い

## 次にやること

**すべて `Set-Location` と環境変数を先に打つこと**（PowerShell 5.1。`&&` は使えない）:

```powershell
Set-Location C:/Users/oshim/Documents/projects/com.-youtube
$env:CLIPPER_CREDENTIALS_DIR = "C:/Users/oshim/Documents/projects/com.-youtube"
```

### 1. 許諾の回答文を突き止める（**優先度が最も高い。運営者の判断**）

Gmail（`oshima6.27@gmail.com`）を 09-04 に探して0件だった。
**投稿を増やす前にここを片づける。**本数を増やすほど risk が線形に増える。

1. 回答はどこに来たのか。別アドレス / 電話 / フォームの画面上 / SNS。
   **来ていないなら `permission.yaml` の `status` を `pending` に戻す**
2. 出てきたら `docs/permission-request.md` に貼り、条件を `conditions` へ書き写す
3. 回答が出てこないなら**送り直すのが筋**。依頼文は `docs/permission-request.md` にある

### 2. 除外2本を削除する（Studio で手動。**取り消せない**）

`cTzbNUFi9Iw` と `hbRhnrvPk-k`。素材 `RGm5F2m12as` 由来で外部の催眠術師が映る。
**今も公開中で、インプレッション63回・72回＝全体の29%を占めている。**
**成績の問題ではなく権利の問題。09-02 から3回持ち越している。**

### 3. 投稿を再開する（**空振りした15枠を組み直す**）

過去日付の枠は `--arm` できない。**まず組み直す:**

```powershell
python scripts/build_schedule.py 2026-09-11 2     # 開始日と1日の本数
python -m clipper schedule                        # 計画を見るだけ
python -m clipper schedule --arm                  # 実際に予約を入れる
```

**`clipper schedule --rebuild` は打たない**（`slots` は8月の実予約の写しで、上書きすると消える）。
手で出すなら `python -m clipper publish <video_id> <clip_id>`。

**測るのはここから。** 9日連続で出したら、新規1本ごとの
**公開後7日のインプレッションとショートフィード比率**を見る。
再生数ではなく配信側で見ること。**いま比較できる基準値は「1本あたり29回/28日・フィード4.7%」。**

### 4. 尺の実験を設計する（**3が動いてから**）

こちらは13本すべて 49〜68秒、市場の中央値は31秒。
**「短くすれば伸びる」根拠は無い**（競合72本で r=0.050）。
一方でスワイプ離脱が37%あり、**完走率のほうは尺と機械的に結びつく。**
在庫を出すときに **≤35秒と55〜68秒を交互に**出せば、追加コストなしで比較できる。
`config/settings.yaml` の `max_seconds: 68` は tora-kirinuki 側の実測で、**この市場で測り直していない。**

### 5. 旧型式のタイトル6本を直す

```powershell
python -m clipper retitle <video_id> <clip_id> --title "<本文>" --members <確認した名前>
```

50ユニット／本。動画IDも再生数も維持される。
`cTzbNUFi9Iw` / `hbRhnrvPk-k` は削除予定なので直さない。

### 6. `M7ZxIL_b39E` の8本をアップロードするか決める（**運営者の判断**）

書き出しと第三者点検は済み（`out/M7ZxIL_b39E_auto0*_short.mp4`）。
**保留の理由は在庫十分・クォータ未確認・許諾の裏づけ無しの3つ。**
在庫は依然18本あるので、**3 を先に動かすほうが順番として正しい。**

出すときは1本ずつ。`--members` には映像で確認できた名前だけ:

```powershell
python -m clipper upload M7ZxIL_b39E auto04 --title "<本文>" --members やまと,ゆうた,ひゅうが,ゆうま,あむぎり
```

## 触ってはいけないところ

### 権利

- **コラボ回・ファン参加回・イベント回は第三者の肖像の問題で使えない。**
  許諾を依頼したのは BRDOCK だけで、第三者はその射程外
- **`--members` に推測を渡さない。** 映像で確認したものだけ。名簿外は弾かれるが、
  **名簿内の別人の名前は弾けない**
- **自動判定を通っても使えるとは限らない。実測で4回外している**
  （`fbKne9hTmgA` 体育館の一般の方 / `MgO0lCUtlx4 auto01` 高校対中学の試合 /
  `RGm5F2m12as` 外部の催眠術師 / `zvM7bkbavDQ` ファン参加）。
  **書き出したら必ず `audit_third_parties.py` でコマを見る**
- 動画ごと除外: `RGm5F2m12as` / `fbKne9hTmgA` / `ARuwTdvqJJA` / `lCKD3eRA6nE`
- `Fb9bO8V9oNA`（目隠しかくれんぼ）は CDF シャッフルコラボ回。素材にしない
- `abW8zkEwEW4/auto03` は下ネタのため素材にしない

### 運用

- **`clipper schedule --rebuild` を打たない**（上の「次にやること 3」）
- **1日の公開は2本まで**（`settings.yaml` の `max_publish_per_day`）。gate が数えている。
  gate の当日上限は**スロットの日付ではなく「今日」の公開本数**を見るので、
  今日2本出したあとは未来日付のスロットまで巻き添えで止まる。
  **公開を増やすために安全弁を緩める変更は勝手に入れない**
- **収益化を ON にすると gate が全部止まる**（`conditions.monetization` が `allowed` 以外のため）。
  `channel.monetization_enabled` は API では取れないので Studio を見て手で書く欄
- カスタムサムネイル不可・15分超不可（電話番号確認ができないため）。**再依頼しない**

### 認証とリポジトリ

- **`token.json` / `client_secret.json` を退避するとき、リポジトリ内に置かない。**
  `.gitignore` は `token.json*` などに広げたが、**別名（例: `auth_backup.json`）にすれば
  素通りする**。退避先はリポジトリの外にすること
- **このリポジトリは PUBLIC。** `git add -A` の前に `git status --short` で中身を見る
- 端末は **Windows PowerShell 5.1**。`VAR=値 コマンド` と `&&` はどちらも使えない
  （`&&` はパースエラーで**行ごと何も実行されない**）。環境変数は `$env:NAME = "値"` を
  別行で、逐次実行は `;` でつなぐ。**Claude 側の Bash ツールは Git Bash なので書き分けること**
