# HANDOFF

最終更新: 2026-08-27

## いま何をしているのか

コムドット切り抜きの制作と予約管理。**このセッションは最新動画 `xBpumDn8QYE` を調査し、
ショート1本を書き出したところまで。アップロードも予約も入れていない（入れられない）。**

依頼は「最新の動画を調査 → 切り抜きを作成 → 予約投稿」。**3つ目は物理的に不可能。**
理由は下の「検証済みの事実 A」。前回セッションの宿題（除外2本の削除・API復旧・台帳の
突き合わせ・許諾の確認）は**まだ1つも片付いていない。**

## 今回やったこと（2026-08-27）

1. `@comdot` の最新12本を `yt-dlp --flat-playlist` で取得し、最新が `xBpumDn8QYE` と確認
2. `screen.screen()` を最新4本にかけた
3. `clipper candidates xBpumDn8QYE --count 6` で候補6件を取得
4. 候補6件それぞれの**字幕本文を読み**、映像フレームを抜いて**目で見た**
5. 上位候補を採らず、**手で区間を選び直して**ショート1本を書き出した
6. `data/videos/xBpumDn8QYE.json` に台帳を作成（clip_id `man01`、notes 2件）

変更したファイル: `data/videos/xBpumDn8QYE.json`（新規）、`HANDOFF.md`
書き出し: `out/xBpumDn8QYE_man01_short.mp4`（git 追跡外）

## 検証済みの事実（実際に画面に出た出力のみ）

### A. 予約投稿は2重にブロックされている。今回は入れていない

**A-1. API の認証が死んだまま。** `upload.get_service()` の出力:

```
UploadBlocked: リフレッシュトークンが失効しています（invalid_grant）。
```

`token.json` は 2026-08-14 09:41 のまま。復旧は**人がブラウザで同意画面を通す**必要があり、
このセッションでは不可能。

**A-2. 許諾が pending なので gate が止める。** 今回作ったクリップの gate 判定:

```
gate: held ['許諾ステータスが pending（granted ではない）']
```

`schedule.py` の冒頭にこう書いてある —「`publishAt` を設定すると YouTube が指定時刻に
自動で公開する。許諾の回答が来なくても、断られても公開される。BRDOCK へ
『ご許諾をいただけた場合にのみ公開いたします』と伝えている以上、回答前に仕掛けてはいけない」。
**gate を迂回する形で予約は入れていない。**

### B. 最新動画 `xBpumDn8QYE` の実態

```
【乾杯バニー再来】2泊3日無限にお酒飲み続ける旅行で最高すぎる男の思い出できたwwwwww
公開 2026-08-26 / 01:39:31 / screen.screen() → ok=True（機械判定は通過）
信号: コメント言及 69箇所 / 語彙 419件 / ヒートマップ 0区間
```

- ひゅうが（金髪）とゆうまの2人（ヤニヤニ）による北海道旅行**後編**。前編は `G2gXaVSnOQY`
- **全編が飲酒。** すすきのの店とその道中で、2人とも泥酔する場面が続く
- 概要欄に「北海道で出会って友達になったみんな」とある

### C. 自動候補の上位は使えなかった（字幕とフレームで確認）

`candidates` の上位6件のうち **4件が一般の方との路上でのやり取り**だった。
字幕本文を読んで確認した実例:

| 候補 | 区間 | 中身 |
|---|---|---|
| 1 | 23:05-24:11 | 「あっちゃん北海道の可愛い女の子」「気をつけて帰ってください」＝ファンとの会話 |
| 2 | 17:00-18:07 | 「めっちゃ歌聞いてます」「ありがとうございました」＝ファンとの会話で始まる |
| 3 | 24:23-25:29 | 通行人・タクシーとのやり取り |
| 6 | 15:49-16:53 | 「写真だけダメなんだ。握手までは」＝ファンとの会話 |

`fbKne9hTmgA`（一般の方の映り込み）・`RGm5F2m12as`（外部の催眠術師）を除外したのと**同じ類型**。
**そのまま書き出していたら4本目の取りこぼしになっていた。**

### D. 書き出した1本（手で選び直した区間）

```
clip_id : man01
区間    : 17:24-18:21（57.0秒）
出力    : out/xBpumDn8QYE_man01_short.mp4  1080x1920  h264/aac  44.7MB
gate    : held（許諾 pending）
```

- 候補2の区間から**ファンとの会話（〜17:16）を切り落として**始点を 17:24 に寄せた
- 終点 18:21 は「ちょっと俺タバコ吸うから」の直前
- コメントの裏付け: 17:57 と 18:00 にそれぞれ2件。「完全再現やん爆笑」
  「コニー母になったゆうまちゃんを片手で支えれるなんて、ひゅうが君逞しい」
- **第三者点検済み。** 17:26 / 17:36 / 17:46 / 17:57 / 18:06 / 18:16 のフレームを抜いて目視。
  メンバー2人以外に特定できる人物は映っていない（遠景の通行人のみ）
- 焼き込みテロップを実見して確認した文言:「どうもコニー母です」「2軒目」
  （ASR は「こには」「スプリンガー母」と崩していた。ASR は採らなかった）
- 帯の文言（`hook` / `footer`）:
  - 上「潰れた相方を抱え上げたら完成した図」
  - 下「コムドットのひゅうが（金髪）とゆうまによる2泊3日の北海道旅行の後編。
    すすきので飲み歩いた道中、酔って動けなくなったゆうまをひゅうがが抱え上げた場面」
- 想定タイトル（台帳の `planned_title`。**まだ使っていない**）:
  「酔い潰れた相方を抱え上げたら完成した画が面白すぎる #コムドット #コムドット切り抜き」

## 未検証のもの

- **この素材を使ってよいかの最終判断はしていない。** 全編が飲酒で、書き出した区間も
  泥酔した相方を抱える場面。YouTube 側の年齢制限を受けるかどうかは**確かめていない**
- **`exclusions.yaml` に `xBpumDn8QYE` を足していない。** 一般の方が映るのは上位候補の区間で
  あって動画全体ではないため、`fbKne9hTmgA` / `RGm5F2m12as` と同じ「動画ごと除外」に
  すべきかは判断を保留した
- 前回セッションの宿題 1〜5（除外2本の削除、API復旧、台帳の突き合わせ、許諾の確認、
  過去のアカウント削除の影響）は**すべて未着手のまま**
- 書き出した mp4 は**通しで再生していない。** 確認したのは 1秒 / 33秒 / 55秒のフレームのみ

## 次にやること

### 0. 書き出した1本を通しで見る（人）

```bash
start "" "C:\Users\oshim\Documents\projects\com.-youtube\.claude\worktrees\video-creation-scheduling-2370b5\out\xBpumDn8QYE_man01_short.mp4"
```

飲酒の映り方を見て、**この素材を使うかどうかを決める。** 使わないなら
`config/exclusions.yaml` の `video_ids` に `xBpumDn8QYE` を足して台帳に理由を書く。

### 1. 除外2本を公開停止する（運営者が Studio で手動。API は死んでいる）

`cTzbNUFi9Iw` と `hbRhnrvPk-k`。素材 `RGm5F2m12as` 由来で、外部の催眠術師が映っている。
非公開に戻すだけでは次の一括操作でまた巻き込まれるので**削除する**。

### 2. API を復旧する（人がブラウザで行う。無人では通らない）

```bash
cd C:/Users/oshim/Documents/projects/com.-youtube && rm token.json && python -m clipper auth
```

同意画面では必ず「コムドット名場面ch【切り抜き】」を選ぶ。`UCoT2TYsxzH4t42C2oF-KrAw` が
表示されれば正しい。**7日でまた失効する。**

### 3. 台帳を実態に合わせる（API 復旧後）

`data/videos/*.json` は20本すべて `private` と記録しているが、実際は14本が公開済み。
`config/schedule.yaml` の `not_scheduled` の2本も公開済み。**このずれを残したまま
`schedule --rebuild` や一括操作をしない。**

### 4. 許諾の扱いを決める

依頼文は「ご許諾をいただけた場合にのみ公開いたします」だが、pending のまま14本が公開中。
Gmail に BRDOCK とのやり取りの記録が無い。**依頼を本当に送ったのかを確認する。**

### 5. アップロードと予約（1〜4が片付いてから）

```bash
cd C:/Users/oshim/Documents/projects/com.-youtube
python -m clipper upload xBpumDn8QYE man01 --title "酔い潰れた相方を抱え上げたら完成した画が面白すぎる #コムドット #コムドット切り抜き"
python -m clipper schedule            # 計画を見るだけ。arm しない
```

`schedule --arm` は **`permission.yaml` が `granted` になるまで打たない。**

## 触ってはいけないところ

- **gate を迂回して publishAt を入れない。** Studio での手動予約も運営者の判断領域
- `RGm5F2m12as` / `fbKne9hTmgA` は動画ごと除外（`config/exclusions.yaml`）。
  **RGm5F2m12as 由来の2本は現在公開されてしまっている（上記1）**
- `Fb9bO8V9oNA`（目隠しかくれんぼ）は CDF シャッフルコラボ回。素材にしない
- カスタムサムネイル不可・15分超不可（チャンネルの電話番号確認ができないため）。再依頼しない
- **`xBpumDn8QYE` の自動候補（auto01〜auto06 相当）をそのまま書き出さない。** 上位4件は
  一般の方との路上のやり取り。台帳に残してあるのは手で選び直した `man01` だけ
- `wKuTNfA6Xhg` auto03 は区間取得・第三者点検まで済み、**未書き出し・未アップロード**
