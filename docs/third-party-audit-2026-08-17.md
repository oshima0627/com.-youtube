# 第三者の映り込み点検（2026-08-17）

予約計画に入っていた8本と、在庫の候補について、区間へ等間隔にコマを散らして
実際に見た。手順は `scripts/audit_third_parties.py`。

**きっかけ**: 差し替え候補 `RGm5F2m12as clip01/clip02` のコマを見たら、外部の
催眠術師が画面の主役だった。同じ動画の `auto01`（予約済み）も同様に映っていた。
`screen.py` / `gate.py` は概要欄にコラボの印が無いため通していた。

許諾を依頼したのは株式会社BRDOCK だけで、**第三者の肖像・実演はその射程外。**
以降、書き出したクリップは必ずこの点検を通す。

## 結果

| 動画 / クリップ | 尺 | コマ | 映っている人 | 判定 |
| --- | ---: | ---: | --- | --- |
| `51tyFgelkmQ` auto01 | 58.5s | 6 | 室内。メンバー3人のみ | 可 |
| `51tyFgelkmQ` auto02 | 64.3s | 6 | 車内と住宅街の路上。通行人の映り込みなし | 可 |
| `51tyFgelkmQ` wide01 | 684.0s | 12 | 室内。メンバー3人のみ | 可 |
| `MgO0lCUtlx4` auto02 | 65.9s | 6 + 全解像度3 | 体育館。BIRDOG のユニフォーム5人のみ、観客なし | 可 |
| `nZSBkNRFsDQ` auto01 | 67.9s | 6 | 車内。メンバーのみ | 可 |
| `nZSBkNRFsDQ` auto02 | 67.7s | 4 | 車内。メンバーのみ | 可 |
| `wKuTNfA6Xhg` auto02 | 66.5s | 6 | 室内。メンバー4人と画面のアバター | 可 |
| `RGm5F2m12as` auto01 | 57.6s | 4 | 外部の催眠術師が中央に立つ | **除外** |
| `RGm5F2m12as` clip01 | 57.0s | 4 | 同上。画面の主役 | **除外** |
| `RGm5F2m12as` clip02 | 39.2s | 4 | 同上。画面の主役 | **除外** |
| `abW8zkEwEW4` auto01 | 59.4s | 6 | 車内。メンバー2人のみ | 可 |
| `abW8zkEwEW4` auto02 | 66.7s | 6 | キッチン。メンバー2人のみ | 可 |

`MgO0lCUtlx4` は同じ動画の `auto01` が高校対中学の試合で既に除外済み。`auto02`
は試合とは別の区間（00:47 台の体幹）で、コートに5人しかいないことを全解像度の
コマでも確かめた。

## 残っている手当て

`RGm5F2m12as` の2本は**既に非公開でアップロード済み**。

| クリップ | YouTube ID |
| --- | --- |
| auto01 | `cTzbNUFi9Iw` |
| auto02 | `hbRhnrvPk-k` |

YouTube 側からの削除は手作業。残したまま `clipper schedule --arm` すると
公開されうるので、予約計画からは枠ごと外してある（`config/schedule.yaml` の
`vacancies`）。

## 追加分（2026-08-18）

新しく書き出した `bCKjVkvfJIM`（【大波乱】コムドット初のトリオ人気投票）の2本に
同じ点検を通した。

| 動画 / クリップ | 尺 | コマ | 映っている人 | 判定 |
| --- | ---: | ---: | --- | --- |
| `bCKjVkvfJIM` auto01 | 68.0s | 6 | 室内。メンバー5人のみ | 可 |
| `bCKjVkvfJIM` auto02 | 68.0s | 6 | 室内。メンバー5人のみ | 可 |

区間はどちらも同じ部屋でのトーク。背景は壁で、映り込む人物はいない。
auto02 の第3位発表で画面左下に出る3人の写真もメンバー本人。

素材選びの時点で、第三者が確実に入る回（外部クリエイターとの企画、ファン参加、
外部の演者、屋外の観客）は候補から外した。`work/source_pool.md` の○のうち、
`ik-HVr4wpoA`（ばんばんざいみゆ）、`Z-sEr8kUy-c`（ViViモデル）、
`rTUwp444tm8`（単独LIVE）、`0e9a-q5UPl0`（お渡し会）、`CTBio9oRIrs`（元日本代表）、
`DQCf1QD001Y`（総勢8組）は、**タイトルの時点で第三者が入ると読めるので使わない。**

## 追加分（2026-08-18・2回目）

`8DDHTuwdbyQ`（【重大発表あり】やまとの誕生日パーティーやまと抜き＝本人不在飲み会）。

| 動画 / クリップ | 尺 | コマ | 映っている人 | 判定 |
| --- | ---: | ---: | --- | --- |
| `8DDHTuwdbyQ` auto01 | 67.0s | 6 | 飲食店の個室。メンバー4人のみ。店員・他の客の映り込みなし | 可 |
| `8DDHTuwdbyQ` auto02 | 65.0s | 6 | 同上（第三者はいない） | **除外**（別の理由） |

**auto02 は第三者ではなく中身の理由で落とした。** 区間の前半がメンバー本人への
指摘（「プライベートとしての感情で尖っちゃってる部分があるならそこは削っても
いいんじゃないか」）で、本編では直後のわさびの話に流れて笑いに変わるが、
68秒で切り出すと指摘だけが残る。名誉を害する編集にあたりうる。

**第三者の点検を通っても使えるとは限らない。** 点検シートは誰が映っているかしか
見ないので、発言の中身は別に読む必要がある。`config/exclusions.yaml` の
`blocked_terms`（訴訟・逮捕・謝罪・炎上）はこの種の指摘を拾わない。
